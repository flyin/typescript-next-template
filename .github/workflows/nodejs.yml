name: Node CI

on: 
  push:
    branches:
    - master
    - release/*

  pull_request:
    branches:
    - master

jobs:
  checkout:
    container: 
      image: node:latest
      env:
        NODE_ENV: development
    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js
    - name: Install packages
      run: |
        npm install
